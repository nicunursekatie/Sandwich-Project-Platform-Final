### Overview
This full-stack application for The Sandwich Project nonprofit streamlines sandwich collections, donations, and distributions. It provides comprehensive data management, analytics, and operational tools for volunteers, hosts, and recipients. The project enhances data visibility, supports organizational growth, and is a vital tool for food security initiatives, aiming to reduce food waste and hunger. The annual sandwich goal is runtime-configurable via the `app_settings` table (see App Settings entry below).

### User Preferences
Preferred communication style: Simple, everyday language.
UI Design: Button labels and interface text must be extremely clear about their function - avoid ambiguous labels like "Submit" in favor of specific action descriptions like "Enter New Data".
Form Design: Eliminate redundant or confusing form fields - host dialogs should have a single "Host Location Name" field instead of separate "Name" and "Host Location" fields.
Mobile UX Priority: Mobile user experience is critical - chat positioning and space efficiency are key concerns. Vehicle type should NOT be required for new driver entries.
Documentation: All technical findings and fixes must be documented in replit.md to avoid repeated searching and debugging.
Analytics Philosophy: NEVER compare or rank hosts against each other. The Sandwich Project is about increasing volunteer turnout globally, not about which host reported more/less sandwiches. All host comparison features, "top performing hosts", "underperforming hosts", and similar language must be removed from analytics.
Desktop Chat UX: Desktop users require proper scrolling behavior without nested scrolling containers that cause page focus issues - chat layout must handle desktop and mobile differently.
Mobile Header Overflow: Dashboard header uses progressive hiding at breakpoints - user info hidden below xs (480px), comments button hidden below xs, OnlineUsers hidden below sm (640px). Button padding reduced on mobile (p-1.5 vs p-2). Header has overflow-x-hidden and max-w-full to prevent any overflow.

### System Architecture
The application features a React 18 frontend with TypeScript, Vite, TanStack Query, and Tailwind CSS (with shadcn/ui). The backend uses Express.js (TypeScript), Drizzle ORM, and PostgreSQL (Neon serverless), including session-based authentication. The UI/UX adheres to The Sandwich Project's official color palette and Roboto typography, prioritizing clarity, responsiveness, and card-based dashboards.

**UI/UX Decisions:**
- Modern, compact design with white card backgrounds, colored left borders for status, warm paper tone page background, subtle shadows, and a strong tonal hierarchy.
- Operational monitoring uses Wednesday-Tuesday week boundaries.
- Visualizations include two-line charts for collection trends and summary cards.
- Purple markers for recipients on maps (distinct from blue events, green hosts).
- "Nearby Recipients" section in right panel showing recipients within 15 miles of selected event.

**Technical Implementations:**
- **Authentication & Permissions**: Role-based access control, session management, password security, and active user enforcement with environment variables controlling modes.
- **Database Configuration**: Centralized database URL selection, specific migration rules for Neon serverless, and careful handling of Drizzle ORM returning clauses.
- **Data Management**: Comprehensive management of collections, hosts, recipients, users, and audit logs with Zod validation, timezone-safe date handling, and soft deletes. `sandwich_collections` is the operational source of truth.
- **Sandwich Totals Calculation**: Correctly sums either `jsonb group_collections` or legacy `group1_count`/`group2_count` fields to prevent double-counting.
- **Van Driver Needed Checkbox Fix**: `vanDriverNeeded`, `isDhlVan`, and `selfTransport` are ALWAYS included in every PATCH payload via `detectChangedFields` in `form-utils.ts`. These boolean flags must never be dropped by change-detection logic — the SpeakerWarningDialog pause (fires for events >500 sandwiches with no speakers) can cause a race condition where the baseline comparison sees no diff and silently drops the field. Fix: `ALWAYS_INCLUDE_FIELDS = new Set(['vanDriverNeeded', 'isDhlVan', 'selfTransport'])` in `detectChangedFields`.
- **Messaging & Notifications**: Email (SendGrid), Socket.IO chat, SMS (Twilio), and dashboard notifications. All outgoing emails BCC'd to `katie@thesandwichproject.org`.
- **Operational Tools**: Project, meeting, and work log management, user feedback, analytics dashboards, and a permissions-based Collection Walkthrough Tool. Event impact reports count actual `sandwichCollections`.
- **Event Requests Management System**: Tracks requests, handles duplicates, manages statuses, integrates with Google Sheets, calculates van driver staffing, supports multi-recipient assignment, intake validation, and interactive Leaflet maps with AI Assistants. Includes auto-save and specific handling for corporate priority and standby follow-ups, along with optimistic lock bypass for contact logs.
- **Event Edit Save Verification (UPDATED May 2026)**: The main EventSchedulingForm no longer treats every 200 response as a clean save. After PATCH, it checks server `_droppedFields` and compares the submitted payload to the returned event data using `findMismatchedSavedFields()` in `client/src/components/event-requests/form-utils.ts`. If any field was dropped or does not round-trip, the form stays open, local auto-save is preserved, event queries are refreshed, and a sticky "Partial Save - Please Review" warning is shown instead of a normal success toast.
- **Verified Event PATCH Helper (UPDATED May 2026)**: Event-related quick edits should use `patchEventRequestVerified()` from `client/src/lib/event-save-verification.ts`, not raw `fetch`, when saving to `/api/event-requests/:id`. The helper calls the normal API helper, checks server `_droppedFields`, compares the PATCH payload to the returned row, and throws a sticky user-facing error if the save did not round-trip. Converted high-risk event edit paths include Driver Planning event edits, Event Map address edits, ScheduledCard quick updates, NewRequest confirmation toggles, and CompletedCard inline edits. On save failure, do not clear the user's edit state.
- **Real-Time Collaboration System**: Multi-user collaboration using Socket.IO for synchronization, presence tracking, field-level locking, threaded comments, and edit revision history.
- **Real-Time Online Presence Notifications**: WebSocket-based instant online presence notifications via Socket.IO with client-side toast notifications.
- **User Activity Logging System**: Comprehensive tracking of authenticated user actions.
- **Sandwich Type Tracking System**: Comprehensive tracking for individual and group collections with real-time validation and analytics.
- **Interactive Route Map & Driver Optimization**: Leaflet map for visualizing host contact locations, route optimization, and driver assignment.
- **Automated Reminders**: 24-hour volunteer reminder system via cron job with configurable email/SMS delivery.
- **TSP Contact Follow-up Notifications**: Automated daily reminders for TSP contacts for approaching 'in_progress' events and toolkit follow-ups.
- **Tiered Notification System**: Three-tier architecture (URGENT, IMPORTANT, DIGEST) to prevent alert fatigue.
- **Corporate 24h Escalation Rate Limiting**: Corporate escalation SMS is rate-limited to once per 24 hours per event, for 'new' or 'in_process' statuses.
- **Notification Status Filtering**: All notification queries exclude inactive event statuses and only include active ones.
- **Smart Follow-up SMS Batching**: SMS notifications are batched per user; emails remain individual.
- **Stale Event Escalation Email Batching**: Escalation emails for stale events are batched into one weekly summary email for specific administrators.
- **SMS Alert Configuration System**: Users can opt-in to SMS notifications, with support for 'hosts' (weekly collection reminders) and 'events' (event coordination) campaign types.
- **Kudos Mark-as-Read System**: Tracks Kudos messages and allows marking them as read via the messaging service.
- **TSP Holding Zone**: Inbox-style system for long-term ideas/tasks with categories, urgent flagging, commenting, likes, assignments, and a three-tier permission system.
- **Guided Tours & Onboarding System**: Interactive, permission-based step-by-step tours for new users.
- **Error Handling & Logging**: Robust error handling with `lazyWithRetry` and improved production-safe logging.
- **Timezone Management**: Ensures accurate storage and display of user-entered times, adhering to `America/New_York`.
- **Google Sheets Sync**: Background service with comprehensive monitoring, alerts, triple deduplication, and message backfill.
- **Google Sheets Sync — Sheet Migration Dedup Bugs (FIXED Apr 2026)**: Three bugs that caused new form submissions to be silently skipped when Squarespace migrated to a new sheet (row IDs reset from 1): (1) **Row-ID false match** — Priority-0 row-ID check matched new entries against old entries from the previous sheet sharing the same row number; fix adds org-name validation before treating it as a duplicate. (2) **Stale externalId false match** — New sheet column J carried over old `sheets-import-*` external_ids from the previous sheet; fix validates org names agree before accepting an exact external_id match as a duplicate. (3) **Null-date false match** — Old-style email-only hash check used `null === null` as proof of a matching event date, blocking any new request from a repeat-contact email that had no desired_event_date; fix returns `false` (not a match) when either date is missing. All three fixes are in `server/google-sheets-event-requests-sync.ts`.
- **React Query Cache Management**: Uses `queryClient.refetchQueries` for immediate UI updates on mutation success.
- **Organization Merge System**: Admin tool to merge duplicate organizations, including similarity scoring, merge preview, and batch updates.
- **Email Template Customization System**: Admins can customize key sections of HTML emails via a UI, supporting placeholders.
- **External API Key Authentication**: Supports API key authentication for external app integrations with role-based permissions and SHA-256 hashed keys.
- **App Settings (Configurable Annual Goal)**: Key/value `app_settings` table backs runtime config. Annual sandwich goal (previously hardcoded 500K) lives at key `annual_sandwich_goal`. Frontend reads via `useAnnualSandwichGoal()` (falls back to `DEFAULT_ANNUAL_SANDWICH_GOAL` from `@shared/schema`). Admin/super_admin/admin_coordinator can edit inline from the Year-end projection card on Analytics → Pace & Comparison. Routes: `GET /api/app-settings` (all auth), `GET /api/app-settings/:key`, `PATCH /api/app-settings/:key` (role-gated). Migration `20260526_add_app_settings.sql` was applied manually to both dev and prod because the pre-existing broken migration `20260315` blocks the auto-runner.
- **SIGTERM / Exit Prevention Bug (FIXED)**: Production mode overrides `process.exit` to prevent accidental library exits, but this also silently swallowed `process.exit(0)` inside the graceful shutdown handler — meaning SIGTERM never actually stopped the server. Old processes held their ports forever, causing EADDRINUSE on restart. Fix: save `realExit = process.exit.bind(process)` BEFORE the override and use it exclusively inside the shutdown function.
- **Neon HTTP Driver — No Transactions (FIXED)**: Production uses Neon's HTTP driver (`drizzle-orm/neon-http`) which throws `Error: No transactions support in neon-http driver` when `db.transaction()` is called. This caused volunteer signups to fail with a 500 error. Fix: removed all `db.transaction()` calls in `server/routes/volunteer-event-hub.ts` (signup creation at line ~682, status update at line ~972) and `server/services/assignments/meeting-project-service.ts` — replaced with sequential `db.*` calls. **Rule: Never use `db.transaction()` anywhere in server code** — the Neon HTTP driver used in production does not support it.
- **Dev Port Configuration**: `Start application` workflow runs `PORT=5000 npm run start`. `Start Server` runs `npm run dev` on port 3000 (changed from 5000 to avoid conflict). Replit proxy forwards the preview URL to port 5000 based on `waitForPort = 5000` in the workflow config. Port 80 is occupied by a Replit system process — do NOT attempt to bind to port 80 from the application. `lsof -ti:80 | xargs kill -9` will kill the Node server (it has a pending socket), so avoid that command.
- **Intake Workflow App Integration**: External app pulls assigned event requests, allows coordinators to complete intake forms, and pushes data back to update events to 'scheduled' status.
- **Document Storage (Cloud)**: Documents stored in Replit Object Storage (Google Cloud Storage) using a two-step presigned URL flow for uploads and cloud streaming for downloads, with fallback for legacy local files.
- **Resource Upload Hang (FIXED May 2026)**: The Reference Materials "Add Resource" dialog (`client/src/components/resource-admin-modal.tsx`) uploaded files by POSTing raw multipart form-data to `/api/documents`. That endpoint was migrated to the cloud two-step presigned-URL JSON flow and has no multipart parser, so the request stalled/hung forever. Fix: the modal now follows the same three-step flow as `client/src/pages/important-documents.tsx` — POST `/api/documents/request-upload-url`, PUT the file to the returned `uploadURL`, then POST `/api/documents` JSON with `objectPath`. Note the create route returns `{ document }`, so read `document.id`. **Rule: never POST file bytes to `/api/documents` — that route only accepts JSON metadata; bytes go directly to the presigned cloud URL.** (This also makes the old workaround of pasting a dev `/attached_assets/...` URL unnecessary — uploaded files are served from cloud storage via `/api/documents/:id`.)
- **Code Health Cleanup Baseline (May 2026)**: Initial cleanup preserved Replit runtime/deployment configuration. `npm run build` passes after changes. `npm run check` still fails on pre-existing TypeScript debt, but the test matcher flood was reduced by excluding `*.test.tsx` from the app type-check, TypeScript now targets `ES2020` to match the Node 20/Replit runtime better, active Google Analytics imports now use `client/src/lib/analytics`, and shared permission helpers now treat non-array permission data as no permissions unless the secure unified checker explicitly handles it. Also fixed runtime-neutral typing drift in shared date parsing, Vite dev-server options, safe object assignment, authenticated request session typing, reminder preference select values, and stale `user.fullName` references. Remaining high-value cleanup areas: Drizzle JSON fields typed as `unknown`, oversized event request card files, storage interface drift, and feature-specific missing/legacy fields.
- **Social Media List Endpoint Field Omission (FIXED May 2026)**: The lightweight `/api/event-requests/list` mapper in `server/routes/event-requests-legacy.ts` (~line 1408–1543) was returning only `socialMediaPostNotes` and silently dropping `socialMediaPostCompleted`, `socialMediaPostRequested`, their date counterparts, and `socialMediaPostLink`. The PATCH route at line ~4137 persisted them correctly, but on refetch the CompletedCard's `request.socialMediaPostCompleted` stayed `undefined`, so clicking "Mark Posted" showed the success toast yet the badge never flipped to "✓ Posted". Fix: added the five fields next to `socialMediaPostNotes` in the lightweight projection (~line 1527). **Rule: when adding any new persisted field that a card UI reads, also add it to the lightweight `/list` mapper** — that endpoint is a deliberate field allow-list, not a full row passthrough.
- **Navigation Reorganization**: Weekly Collections Report and Group Collections Viewer are sub-items under Collections Log in `client/src/nav.config.ts` (the old `collections-reporting` parent was removed).
- **Inline Analytics Tabs**: The dashboard analytics view (`client/src/pages/dashboard.tsx` ~line 733) includes Pace & Comparison (default) and Low/High Weeks tabs alongside the original analytics tabs.
- **Event Requests Sheet Rotation**: The active Squarespace event-requests Google Sheet ID is stored in the `EVENT_REQUESTS_SHEET_ID` secret. When Squarespace publishes to a new sheet, only the secret needs to be updated — the sync reads range `A2:K1000` (row 1 is the header). Manual sync trigger: `POST /api/google-sheets/event-requests/sync/from-sheets`.
- **Event Impact Reports Scheduled/Category Counts (FIXED May 2026)**: The report's default "This Year" preset previously meant year-to-date, so future scheduled events after the current day were excluded from the KPI cards. Current week/month/quarter/year now cover the full named period, and "All Time" includes future scheduled events. The scheduled count and scheduled filter now include both `scheduled` and `rescheduled` statuses. Organization categories are normalized before filtering, charting, exporting, and leaderboards; corporate variants such as `corp`, `corporate`, `company`, `small_medium_corp`, `small/medium business`, `large_corp`, and `large company` are grouped under `Corporate`.
- **Sandwich Range Parsing (FIXED May 2026)**: Text estimates like `500-600`, `500 – 600`, `500 to 600`, or `500 through 600` must be interpreted as a range and stored/reported using the midpoint (`550`), not stripped into `500600`. Shared helper: `shared/sandwich-count-utils.ts`. Google Sheets event-request imports now use this parser and preserve min/max range fields when available. AI impact reports, AI data chat, and Event Impact Reports use range midpoints for reportable counts and ignore suspiciously huge estimated-only event counts (50,000+) so old corrupted values cannot appear as fake record-breaking events.
- **AI Impact Report PDF Text Encoding (FIXED May 2026)**: jsPDF's default Helvetica font cannot render emoji/icons reliably; trend icons like 📈/📉 were printing as mojibake (`Ø=...`) in the "Trends & Insights" section. The PDF route now sanitizes AI-facing PDF text to printable ASCII, strips common mojibake prefixes, replaces emoji trend icons with plain labels (`Growth:`, `Seasonal:`, etc.), and uses ASCII footer text. The AI report prompt also explicitly asks for plain text only.
- **Volunteer Hub Calendar UX (UPDATED May 2026)**: The calendar tab in `client/src/pages/volunteer-event-hub.tsx` no longer relies on tiny truncated event-name chips inside each day. It now uses TSP's warm/bright palette, clear monthly summary stats, day cards with event/open-spot counts, and a selected-day detail panel with full organization, time, location, sandwich count, role openings, and clear sign-up/assignment actions. This is intended for non-technical volunteer users and should stay plain-language and action-focused.
- **Volunteer Hub Calendar Filtering (UPDATED May 2026)**: The calendar view intentionally shows all upcoming Volunteer Hub events, even when the Events/Map views default to "Only show events that need help." Fully staffed events should still appear on the calendar with "Extra help welcome" / needs-met language so volunteers can understand the full schedule without hunting for a small checkbox.
- **Volunteer Hub View Order (UPDATED May 2026)**: Volunteer Hub defaults to the Calendar view for volunteer-friendly browsing. The primary view chooser is ordered Calendar, List, Map, with the old "Events" label renamed to "List" so all three labels describe view styles.
- **Volunteer Hub Banner Readability (UPDATED May 2026)**: Top summary labels in the Volunteer Hub banner use stronger contrast, larger text, and slightly brighter stat cards so non-technical volunteers can read metrics like "Upcoming Events" and "Drivers Needed" comfortably.
- **Volunteer Hub Event Sync (FIXED May 2026)**: Any Event Management mutation must invalidate both `/api/event-requests*` and `/api/volunteer-hub*` query caches. Without this, switching from a scheduled event card to Volunteer Hub could show stale role needs for up to the client stale-time window, such as "Van Driver Needed" after a van driver had just been assigned. The volunteer hub API also treats an uncovered van-driver requirement as an unfilled need and hides it once `assignedVanDriverId` or `isDhlVan` is present.
- **Volunteer Hub Permissions (UPDATED May 2026)**: Volunteer Hub access and actions are separate toggles. `NAV_VOLUNTEER_HUB` opens the page, `EVENT_REQUESTS_SELF_SIGNUP` allows a user to sign themselves up, `EVENT_REQUESTS_ASSIGN_OTHERS` allows assigning another person, and `VOLUNTEER_SIGNUP_APPROVE` allows viewing/managing pending approvals. Driver role approval/assignment also requires `DRIVER_SIGNUP_APPROVE`. These permissions are shown in the common permissions editor with plain-language labels.
- **Grant Metrics YoY Growth (FIXED May 2026)**: The Grant Metrics achievements card no longer hard-codes the growth callout to 2023→2024. It derives the latest completed year-over-year comparison from completed annual totals and summarizes the multi-year pattern across all completed years, while the current in-progress year is called out separately.
- **Grant Metrics Page Duplication Cleanup (UPDATED May 2026)**: The Grant Metrics page was trimmed to reduce repeated storytelling. Duplicate hero stat cards, repeated volunteer/network cards, and a standalone financial-impact card that overlapped with the detailed cost-efficiency section were removed. Key achievement details now live as a compact highlight row inside the year-over-year section.
- **Grant Metrics Volunteer-Hours Assumptions (UPDATED May 2026)**: Estimated participants are calculated as total sandwiches divided by 10, reflecting an assumption of roughly 8-12 sandwiches per participant. The page now states this plainly. Administrative/coordination time was reduced from 3 hours per event to 1.5 hours per event to keep economic-value estimates more conservative.
- **Grant Metrics Ingredient Cost Methodology (UPDATED May 2026)**: The food-value section now explains representative ingredient baselines for deli sandwiches and PB&J. Deli uses Nature's Own Honey Wheat bread, Kirkland sliced cheddar, Kirkland sliced deli turkey, and sandwich bags. PB&J uses the same bread, Jif peanut butter at about 2-3 tbsp per sandwich, and strawberry or grape jelly at about 2 tsp per sandwich. Grant food value currently uses the deli baseline because historical collection data does not yet have a complete deli/PB&J split.
- **Grant Metrics Weekly Need Wording (UPDATED May 2026)**: Recipient weekly need is labeled "Estimated Weekly Bare Minimum Need" and described as a baseline amount for core recipient partners, not the ceiling of community demand. Copy notes that this is likely an undercount and that many recipients can take extra sandwiches during high-output weeks.
- **Grant Metrics Group Event Stat (UPDATED May 2026)**: The Group Events section no longer reports social media engagement percentage because social posts have not been consistently recorded in the app. It also avoids event attendance because attendance is not reliably collected. The replacement stat is average sandwich output per completed group event, based on sandwich counts the app actually tracks.
- **Grant Metrics Technology Cost Context (UPDATED May 2026)**: Removed the unverified claim that a traditional development firm would charge $75K-150K+ for the app. The capacity-building section now focuses on the real ongoing need for technology maintenance, hosting, security updates, integrations, and continued operational improvements.
- **Grant Metrics Number-Free Software Value Language (UPDATED May 2026)**: Technology value copy should avoid specific dollar estimates. Use number-free language: TSP avoided a substantial custom software expense because the operational system was built internally with AI-assisted development and volunteer labor, while still needing ongoing maintenance, hosting, security, integrations, and continued improvements.
- **Grant Metrics Capacity Argument (UPDATED May 2026)**: The capacity-building section no longer claims the next staffing need is a single full-time admin role. It now frames the grant need as paid coordination capacity: operations, group-event coordination, recipient communication, sandwich transportation logistics, reporting, technology upkeep, and follow-up. The page emphasizes that TSP does not need grants for sandwich ingredients, an office, or staff to do the hands-on sandwich-making work; funding the coordination backbone preserves the low-overhead volunteer-powered model. The other concrete infrastructure need remains an additional smaller refrigerated van.

### External Dependencies
- **Database**: `@neondatabase/serverless`, `drizzle-orm`
- **Web Framework**: `express`
- **UI/Styling**: `@radix-ui`, `tailwindcss`, `lucide-react`, `class-variance-authority`, `shadcn/ui`
- **Data Fetching/State**: `@tanstack/react-query`, `react-hook-form`, `zod`
- **Email**: `@sendgrid/mail`
- **Real-time Communication**: `socket.io`, `socket.io-client`
- **PDF Generation**: `pdfkit`
- **Authentication**: `connect-pg-simple`
- **File Uploads**: `multer`, `@uppy/core`, `@uppy/aws-s3`, `@uppy/dashboard`, `@uppy/react`
- **Cloud Storage**: `@google-cloud/storage`
- **Google Integration**: Google Sheets API, Google Analytics
- **Mapping**: `leaflet`, `react-leaflet`, `react-leaflet-cluster`
- **SMS**: `twilio`
