// Minimal test component to check if syntax issue is file-specific
import React from 'react';

export default function DashboardOverviewTest() {
  return (
    <div>
      <h1>Test Dashboard</h1>
    </div>
  );
}